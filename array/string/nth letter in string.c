#include <stdio.h>
#include <string.h>

int main()
{
    int n,i;
    char s[100];
    gets(s);
    scanf("%d",&n);
    for(i=0;i+2<=n;i++)
    {
    }
    printf("%c",s[i]);






    return 0;
}
